// script.js
document.addEventListener('DOMContentLoaded', function () {
  var typed2 = new Typed('#typed2', {
    strings: ['Some <i>strings</i> with', 'Some <strong>HTML</strong>', 'Chars &times; &copy;'],
    typeSpeed: 50,    // Adjust the typing speed here (higher value means slower speed)
    backSpeed: 50,    // Adjust the backspacing speed here (higher value means slower speed)
    fadeOut: true,
    loop: true
  });
});
